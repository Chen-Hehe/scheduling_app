# 排班系统测试报告

## 🎉 测试时间
2026-03-06 20:16 GMT+8

## ✅ 服务启动状态

### 后端服务 (FastAPI)
- **状态**: ✅ 运行中
- **地址**: http://127.0.0.1:8000
- **进程**: PID 8584
- **工作目录**: D:\app_extension\cursor_projects\scheduling-app

### 前端服务 (Vite + React)
- **状态**: ✅ 运行中
- **地址**: http://localhost:5173
- **进程**: PID 21744
- **工作目录**: D:\app_extension\cursor_projects\scheduling-app\front

## ✅ API 测试结果

### 1. 健康检查
```
GET /health
响应：{"status":"ok"}
✅ 通过
```

### 2. 获取花名册状态
```
GET /api/admin/roster/status
响应：
- submitted_members: 1 人（已提交志愿）
- unsubmitted_members: 2 人（未提交）
✅ 通过
```

### 3. 获取排班结果
```
GET /api/admin/schedule
响应：已有排班数据
- 单周 - 周一 - 第一二节：1 人
- 双周 - 周一 - 第三四节：1 人
✅ 通过
```

## 📋 完整测试流程

### 步骤 1: 访问前端页面
打开浏览器访问：**http://localhost:5173**

### 步骤 2: 登录管理后台
1. 点击「管理后台」或访问 `/admin/login`
2. 默认密码（如未修改）：查看 `AdminGuard.tsx` 或 `AdminLogin.tsx`

### 步骤 3: 测试仪表盘功能
- ✅ 查看总成员数统计（应显示 3 人）
- ✅ 查看已提交/未提交列表
- ✅ 查看提交进度条

### 步骤 4: 测试排班功能
1. 切换到「排班操作」标签
2. 点击「一键排班」按钮
3. 应看到 toast 提示：「排班生成成功」
4. 排班表应自动展示

### 步骤 5: 测试手动微调
1. 点击「手动微调」按钮进入编辑模式
2. 点击任意班次单元格
3. 在弹窗中可以：
   - 移除已安排的成员
   - 添加新成员
   - 点击成员姓名打开个人微调窗口
4. 点击成员姓名测试个人微调弹窗
5. 调整完成后点击「确认修改」
6. 应看到 toast 提示：「个人排班调整已保存」

### 步骤 6: 测试导出功能
- 点击「导出 CSV」按钮
- 应下载排班表 CSV 文件

## ⚠️ 注意事项

### 中文乱码问题
数据库返回的中文显示为乱码（如 `??????`），可能是 MySQL 连接编码问题。建议检查：
1. MySQL 服务器字符集设置
2. 数据库 `scheduling_db` 的字符集
3. 连接字符串已包含 `charset=utf8mb4`，可能需要重启 MySQL 服务

### Toast 通知已添加
所有 API 调用都已添加 toast 提示：
- ✅ 成功提示（绿色）
- ❌ 错误提示（红色）
- ℹ️ 信息提示（蓝色）

## 🛠️ 如何停止服务

### 停止后端
```powershell
# 在 PowerShell 中找到进程并停止
Get-Process | Where-Object {$_.Path -like "*scheduling-app*"} | Stop-Process
```

### 停止前端
```powershell
# 同上，或直接关闭终端窗口
```

## 📁 修改的文件清单

1. `front/src/app/App.tsx`
   - 添加 Toaster 组件

2. `front/src/app/components/admin/AdminDashboard.tsx`
   - 添加页面加载时的 useEffect（自动获取后端数据）
   - 完善 handleGenerate（一键排班 API 调用）
   - 完善 handleMemberClick（获取个人排班数据）
   - 完善 handlePersonApply（保存个人调整）
   - 完善 exitEditMode（保存/放弃提示）
   - 修改 PersonalAdjustmentModal 支持后端数据

3. `front/src/app/api.ts`
   - 已存在，封装所有 API 调用

## 🎯 下一步建议

1. **修复 MySQL 中文编码**：确保数据库、表、连接都使用 utf8mb4
2. **添加更多测试数据**：通过 CSV 导入更多成员
3. **测试 C 端提交流程**：测试成员填写志愿的完整流程
4. **添加 E2E 测试**：使用 Playwright 或 Cypress 进行自动化测试

---

**测试完成时间**: 2026-03-06 20:20
**测试人员**: 小龙虾 🦞
